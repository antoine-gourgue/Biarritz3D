# React 19 ecosystem still lags on peer ranges (three/R3F/testing-library),
# and npm's arborist crashes resolving vitest 5's peer graph.
# Relax strict peer resolution for consistent installs (local + CI).
legacy-peer-deps=true
